# Instalação correta do scikit-learn (V3)

**Instale:**
```
pip install scikit-learn
```

**Importe no Python:**
```python
from sklearn import ...
```

> Não use `pip install sklearn` (pacote depreciado).
